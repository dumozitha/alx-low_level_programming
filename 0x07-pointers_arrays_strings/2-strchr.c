#include"main.h"

/**
 * _strchr - locates a character in a string,
 * @s: string input
 * @c: to lacate from
 *
 * Return: first char
 */
char *_strchr(char *s, char c)
{
	while (*s != '\0')
	{
		if (*s == C)
			return (s);
		s++;
	}
	if (&s == c)
		return (s);
	return ('\0');
}
